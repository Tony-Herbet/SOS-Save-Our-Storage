import { StyleSheet } from 'react-native';

export const dividerStyle = StyleSheet.create({
  textContainerCommonStyle: {
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
