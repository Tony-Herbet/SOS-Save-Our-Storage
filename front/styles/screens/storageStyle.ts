import { StyleSheet } from 'react-native';

export const storageStyle = StyleSheet.create({
  bgImage: {
    alignItems: 'center',
  },
  storageContainer: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  storageTitle: {
    color: '#f0c',
  },
});
