import { StyleSheet } from 'react-native';
import { screenContainerStyle } from 'styles/components/screenContainerStyle';

export const productStyle = StyleSheet.create({
  viewContainer: {
    height: '90%',
    position: 'relative',
  },
});
